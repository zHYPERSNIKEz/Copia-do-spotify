#Importacção da biblioteca apenas a que eu vou usar, se importa a bibliteca toda, tenho que usar o nome da biblioteca + . + a função que quero usar
#Ex: math.floor(), caso contario, só usar o nome da parte que usei
from math import floor

numero = float(input('Digite um nuemro: '))

print(floor(numero))